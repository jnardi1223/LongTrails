<!DOCTYPE html>
<html>
  <head>
    <title>Long Trails</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <meta property="og:title" content="AllTrails Clone"/>
    <meta property='og:image' content="<%= image_url('long.jpeg') %>"/>
    <meta property="og:description" content="LongTrails is a clone of AllTrails using Ruby On Rails and React. It has functioning User Authentication and will have Create/Read/Update/Delete functionality on Review and Trail once production is finished."/>
    <meta property="og:url" content="https://long-trails-staging.herokuapp.com/#/"/>
    <link rel="icon" type="image/x-icon" href="<%= image_url('favicon.png') %>" />
    <link href="https://api.mapbox.com/mapbox-gl-js/v2.5.0/mapbox-gl.css" rel="stylesheet">
    <%= stylesheet_link_tag    'application', media: 'all' %>
    <%= javascript_include_tag 'application' %>
    <% if current_user %>
      <script>
        window.currentUser = { 
          "id": "<%= current_user.id %>",
          "first_name": "<%= current_user.first_name %>",
          "email": "<%= current_user.email %>"
          };
      </script>
    <% end %>
  </head>

  <body>
    <%= yield %>
  </body>
</html>
